import React, { ReactElement } from 'react';
import { RouteComponentProps } from '@reach/router';
interface Props {}

export default function Signup(props: RouteComponentProps): ReactElement {
  console.log('signup')
  
  return <div>signup</div>;
}
