import React, { ReactElement } from 'react';
import { RouteComponentProps } from '@reach/router';
interface Props {}

export default function Volunteer(props: RouteComponentProps): ReactElement {
  console.log('volunteer')
  
  return <div>volunteer</div>;
}
